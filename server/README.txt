Project Dependencies:
	node.js
	express
	ws
	
*Node.js must be installed before doing the following

Install Dependencies using:

	npm install

To run server:
	Navigate to server folder and run: 
	
		node app.js